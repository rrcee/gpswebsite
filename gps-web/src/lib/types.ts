export interface Member {
  name: string;
  role: string;
  qualification: string;
  imageUrl?: string;
}

export interface Album {
  title: string;
  cover: string;
  link: string;
  images: string[];
}
